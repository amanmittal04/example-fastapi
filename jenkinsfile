pipeline {
    agent any

    environment {
        DATABASE_HOSTNAME=credentials('20b76092-1966-4f90-96a2-e9b93e07e9ab')
    }

    stages {
        stage('listing env var') {
            steps {
                sh "echo 'My database hostname is $DATABASE_HOSTNAME_PSW    '"
            }
        }
    }
}
